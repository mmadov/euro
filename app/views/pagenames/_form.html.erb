<%= form_for(pagename) do |f| %>
  <% if pagename.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(pagename.errors.count, "error") %> prohibited this pagename from being saved:</h2>

      <ul>
      <% pagename.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %>
    <%= f.text_field :name %>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
